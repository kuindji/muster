/**
 * Stored-record codecs are deliberately introduced with the Task 2 schema.
 * This module exists now so that codec ownership cannot drift into commands.
 */
export const POSTGRES_CODECS_TASK = 2 as const;
